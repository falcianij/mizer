% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tpo2_community.R
\name{enable_tpo2_community}
\alias{enable_tpo2_community}
\title{Enable the temperature-oxygen community extension}
\usage{
enable_tpo2_community(params, forcing, tpo2_pars = list(), g_init = NULL)
}
\arguments{
\item{params}{A \code{MizerParams} object.}

\item{forcing}{A list supplying either \code{T_fun(t)} or \code{T_vec} with \code{times},
and either \code{pO2_fun(t)} or \code{pO2_vec} with \code{times}.}

\item{tpo2_pars}{Named list of parameter overrides. Supported entries are
\code{T_ref}, \code{Q10_M}, \code{Q10_C}, \code{Q10_A}, \code{Q10_O}, \code{a_O}, \code{b_O},
\code{epsilon_SDA}, \code{K_g} and \code{h_g}.}

\item{g_init}{Optional initial lagged oxygen downregulation scalar. Must be
\code{NULL}, a scalar, or a species x size array. If \code{NULL}, it is initialised
to 1 everywhere.}
}
\value{
A modified \code{MizerParams} object with \code{other_params(params)$tpo2}
populated and custom rate functions registered.
}
\description{
Enables a lightweight temperature-oxygen (tpo2) extension for an existing
\code{MizerParams} object. The extension keeps the standard mizer encounter logic,
but replaces feeding-level, predation-rate and energy calculations with
temperature- and oxygen-aware rate functions. Standard mizer behaviour remains
unchanged unless this function is called.
}
\details{
The extension stores all of its settings in \code{other_params(params)$tpo2},
including user-editable defaults for temperature and oxygen parameters,
forcing data, and the lagged oxygen downregulation scalar \code{g_prev}.
}
\examples{
params <- newCommunityParams()
forcing <- list(
    times = c(0, 5),
    T_vec = c(10, 10),
    pO2_vec = c(20, 20)
)
params_tpo2 <- enable_tpo2_community(params, forcing = forcing)
}
